{
  "buildCommand": "pnpm run build",
  "outputDirectory": ".",
  "rewrites": [{ "source": "/mcp", "destination": "/api/index" }],
  "functions": {
    "api/index.ts": {
      "maxDuration": 60
    }
  }
}
